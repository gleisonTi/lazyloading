import { RemoteBindingDirective } from './remote-binding.directive';

describe('RemoteBindingDirective', () => {
  it('should create an instance', () => {
    const directive = new RemoteBindingDirective();
    expect(directive).toBeTruthy();
  });
});
